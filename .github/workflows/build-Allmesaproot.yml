# build-mesaproot.yml
name: Build All-Mesa for proot (Multi-arch)

on:
  workflow_dispatch:
    inputs:
      mesa_branch:
        description: 'Mesa 分支 (可选，默认为 adreno-main)'
        required: false
        default: 'adreno-main'
        type: string
      use_upstream:
        description: '是否使用上游官方 Mesa (而不是 lfdevs 的分支)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      target_arch:
        description: '目标架构'
        required: false
        default: 'both'
        type: choice
        options:
          - 'aarch64'
          - 'arm-linux-gnueabihf'
          - 'both'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
    - name: 设置构建参数
      id: setup
      run: |
        # 设置默认参数
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "MESA_BRANCH=${{ github.event.inputs.mesa_branch }}" >> $GITHUB_ENV
          echo "USE_UPSTREAM=${{ github.event.inputs.use_upstream }}" >> $GITHUB_ENV
          echo "TARGET_ARCH=${{ github.event.inputs.target_arch || 'both' }}" >> $GITHUB_ENV
        else
          echo "MESA_BRANCH=adreno-main" >> $GITHUB_ENV
          echo "USE_UPSTREAM=false" >> $GITHUB_ENV
          echo "TARGET_ARCH=both" >> $GITHUB_ENV
        fi
        
        BUILD_DATE=$(date +%Y%m%d)
        echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_ENV
        
        # 设置安装目录
        echo "INSTALL_PREFIX=/opt/mesa" >> $GITHUB_ENV
        
        echo "使用的 Mesa 分支: $MESA_BRANCH"
        echo "是否使用上游: $USE_UPSTREAM"
        echo "目标架构: $TARGET_ARCH"
        echo "安装目录: $INSTALL_PREFIX"
        echo "构建日期: $BUILD_DATE"

    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 显示环境信息
      run: |
        echo "构建环境信息:"
        uname -a
        lscpu | grep -E 'Model name|Architecture|CPU MHz|Core'
        free -h
        echo "当前架构: $(uname -m)"
        echo "目标架构: $TARGET_ARCH"

    - name: 安装系统依赖
      run: |
        sudo sed -i "s/^Types: deb$/Types: deb deb-src/" /etc/apt/sources.list.d/ubuntu.sources
        sudo apt update
        
        # 基础依赖（用于 aarch64 构建）
        sudo apt install -y \
          glslang-tools libxrandr-dev libxxf86vm-dev libxcb-shm0-dev libxcb-glx0-dev \
          libxext-dev llvm-19 libx11-dev libx11-xcb-dev libxcb1-dev libxcb-dri3-dev \
          libxcb-present-dev libxcb-randr0-dev libxcb-sync-dev libxcb-xfixes0-dev \
          libxshmfence-dev libdrm-dev libgbm-dev libegl1-mesa-dev libexpat1-dev \
          libwayland-dev python3-pip python3-setuptools python3-wheel ninja-build \
          flex bison libtool pkg-config
        
        # 如果包含 armhf 构建，安装交叉编译工具链
        if [ "$TARGET_ARCH" = "both" ] || [ "$TARGET_ARCH" = "arm-linux-gnueabihf" ]; then
          echo "安装 ARM 交叉编译工具链..."
          sudo apt install -y \
            gcc-arm-linux-gnueabihf \
            g++-arm-linux-gnueabihf \
            binutils-arm-linux-gnueabihf \
            libdrm-dev:armhf \
            libx11-dev:armhf \
            libxext-dev:armhf \
            libxfixes-dev:armhf \
            libxcb1-dev:armhf \
            libxcb-dri3-dev:armhf \
            libxcb-present-dev:armhf \
            libxcb-sync-dev:armhf \
            libxshmfence-dev:armhf \
            libxxf86vm-dev:armhf \
            libexpat1-dev:armhf \
            libwayland-dev:armhf
        fi

    - name: 安装 Python 构建工具
      run: |
        sudo pip3 install meson mako
        
        echo "检查安装的 meson 版本:"
        meson --version
        python3 -c "import mesonbuild; print('mesonbuild 模块路径:', mesonbuild.__file__)"
        
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: 克隆 Mesa 源码
      run: |
        echo "📥 克隆 Mesa 仓库..."
        
        if [ "$USE_UPSTREAM" = "true" ]; then
          echo "使用上游官方 Mesa 仓库"
          git clone https://gitlab.freedesktop.org/mesa/mesa.git
          cd mesa
        else
          echo "使用 lfdevs 的 Mesa for Android Container 仓库"
          git clone https://github.com/lfdevs/mesa-for-android-container.git mesa
          cd mesa
          
          # 检出指定分支
          if [ -n "$MESA_BRANCH" ] && [ "$MESA_BRANCH" != "main" ]; then
            echo "检出分支: $MESA_BRANCH"
            git checkout $MESA_BRANCH
          fi
        fi
        
        # 获取版本信息
        GIT_HASH=$(git rev-parse --short HEAD)
        if [ -f "VERSION" ]; then
          OFFICIAL_VERSION=$(cat VERSION)
        else
          OFFICIAL_VERSION="git-$GIT_HASH"
        fi
        
        GIT_DESCRIBE=$(git describe --tags --always 2>/dev/null || echo "custom-$GIT_HASH")
        
        echo "MESA_GIT_HASH=$GIT_HASH" >> $GITHUB_ENV
        echo "MESA_VERSION=$OFFICIAL_VERSION" >> $GITHUB_ENV
        echo "GIT_DESCRIBE=$GIT_DESCRIBE" >> $GITHUB_ENV
        
        echo "✅ 源码克隆完成"
        echo "版本: $OFFICIAL_VERSION"
        echo "提交: $GIT_HASH"
        echo "描述: $GIT_DESCRIBE"
        echo "分支: $(git branch --show-current)"

    - name: 构建 aarch64 架构
      if: env.TARGET_ARCH == 'aarch64' || env.TARGET_ARCH == 'both'
      run: |
        echo "🔨 开始构建 aarch64 架构..."
        cd mesa
        
        # 为 aarch64 创建构建目录
        mkdir -p build-aarch64
        
        # 配置 aarch64 构建
        meson setup build-aarch64 \
           -Dprefix=$INSTALL_PREFIX \
           -Dplatforms=x11 \
           -Dbuildtype=release \
           -Degl-native-platform=x11 \
           -Dvulkan-beta=true \
           -Dgallium-extra-hud=true \
           -Dgallium-drivers=freedreno,zink \
           -Dmediafoundation-codecs=all \
           -Dvulkan-drivers=freedreno \
           -Dfreedreno-kmds=kgsl \
           -Dshader-cache=enabled \
           -Dgles1=enabled \
           -Dgles2=enabled \
           -Dopengl=true \
           -Dgbm=enabled \
           -Dglx=dri \
           -Degl=enabled \
           -Dglvnd=enabled \
           -Dmicrosoft-clc=disabled \
           -Dllvm=enabled \
           -Dshared-llvm=enabled \
           -Dvideo-codecs=all
        
        echo "🏗️  正在构建 aarch64..."
        ninja -C build-aarch64 -j$(nproc)
        
        echo "📦 正在安装 aarch64..."
        sudo ninja -C build-aarch64 install
        
        # 验证安装
        echo "aarch64 安装验证:"
        ls -la $INSTALL_PREFIX/lib/
        echo "aarch64 库文件架构:"
        file $INSTALL_PREFIX/lib/*.so 2>/dev/null | head -5 || true

    - name: 构建 arm-linux-gnueabihf 架构
      if: env.TARGET_ARCH == 'arm-linux-gnueabihf' || env.TARGET_ARCH == 'both'
      run: |
        echo "🔨 开始构建 arm-linux-gnueabihf 架构..."
        cd mesa
        
        # 创建交叉编译配置文件
        cat > cross-file.txt << EOF
            [binaries]
            c = 'arm-linux-gnueabihf-gcc'
            cpp = 'arm-linux-gnueabihf-g++'
            ar = 'arm-linux-gnueabihf-ar'
            strip = 'arm-linux-gnueabihf-strip'
            pkgconfig = 'pkg-config'

            [host_machine]
            system = 'linux'
            cpu_family = 'arm'
            cpu = 'armv7l'
            endian = 'little'

            [properties]
            needs_exe_wrapper = true

            [paths]
            prefix = '$INSTALL_PREFIX'
        EOF
        
        # 为 armhf 创建构建目录
        mkdir -p build-armhf
        
        # 设置交叉编译环境变量
        export CC="arm-linux-gnueabihf-gcc"
        export CXX="arm-linux-gnueabihf-g++"
        export AR="arm-linux-gnueabihf-ar"
        export STRIP="arm-linux-gnueabihf-strip"
        export PKG_CONFIG_PATH="/usr/lib/arm-linux-gnueabihf/pkgconfig"
        export PKG_CONFIG_SYSROOT_DIR="/usr/arm-linux-gnueabihf"
        
        # 配置 armhf 构建
        meson setup build-armhf \
           --cross-file cross-file.txt \
           -Dprefix=$INSTALL_PREFIX \
           -Dplatforms=x11 \
           -Dbuildtype=release \
           -Degl-native-platform=x11 \
           -Dvulkan-beta=true \
           -Dgallium-extra-hud=true \
           -Dgallium-drivers=freedreno,zink \
           -Dmediafoundation-codecs=all \
           -Dvulkan-drivers=freedreno \
           -Dfreedreno-kmds=kgsl \
           -Dshader-cache=enabled \
           -Dgles1=enabled \
           -Dgles2=enabled \
           -Dopengl=true \
           -Dgbm=enabled \
           -Dglx=dri \
           -Degl=enabled \
           -Dglvnd=enabled \
           -Dmicrosoft-clc=disabled \
           -Dllvm=enabled \
           -Dshared-llvm=enabled \
           -Dvideo-codecs=all
        
        echo "🏗️  正在构建 armhf..."
        ninja -C build-armhf -j$(nproc)
        
        echo "📦 正在安装 armhf..."
        sudo ninja -C build-armhf install
        
        # 验证安装
        echo "armhf 安装验证:"
        ls -la $INSTALL_PREFIX/lib/
        echo "armhf 库文件架构:"
        file $INSTALL_PREFIX/lib/*.so 2>/dev/null | head -5 || true

    - name: 重命名库目录（如果构建了双架构）
      if: env.TARGET_ARCH == 'both'
      run: |
        echo "🔄 重新组织库目录结构以适应双架构..."
        
        # 检查是否存在两个架构的库
        cd $INSTALL_PREFIX
        
        if [ -d "lib" ]; then
          # 检查 lib 目录中的文件类型
          echo "当前 lib 目录中的文件架构:"
          file lib/*.so 2>/dev/null | grep -E "ELF 64-bit|ELF 32-bit" || true
          
          # 分离 64 位和 32 位库
          mkdir -p lib64 lib32
          
          # 移动 64 位库到 lib64
          for file in lib/*.so*; do
            if [ -f "$file" ]; then
              if file "$file" | grep -q "ELF 64-bit"; then
                mv "$file" lib64/
                echo "移动 64 位库: $(basename $file)"
              elif file "$file" | grep -q "ELF 32-bit"; then
                mv "$file" lib32/
                echo "移动 32 位库: $(basename $file)"
              fi
            fi
          done
          
          # 移动子目录
          if [ -d "lib/dri" ]; then
            # 检查 dri 目录中的文件
            mkdir -p lib64/dri lib32/dri
            for file in lib/dri/*.so; do
              if [ -f "$file" ]; then
                if file "$file" | grep -q "ELF 64-bit"; then
                  mv "$file" lib64/dri/
                elif file "$file" | grep -q "ELF 32-bit"; then
                  mv "$file" lib32/dri/
                fi
              fi
            done
            rmdir lib/dri 2>/dev/null || true
          fi
          
          # 清理空目录
          rmdir lib 2>/dev/null || true
          
          # 创建符号链接以保持兼容性
          mkdir -p lib
          ln -sf ../lib64 lib/aarch64-linux-gnu
          ln -sf ../lib32 lib/arm-linux-gnueabihf
          
          echo "✅ 库目录重组完成"
          echo "目录结构:"
          ls -la
          tree -L 2 2>/dev/null || echo "安装 tree 命令以查看目录树"
        fi

    - name: 打包构建产物
      run: |
        echo "📦 开始打包..."
        
        # 创建版本化的包名
        if [ "$TARGET_ARCH" = "both" ]; then
          PACKAGE_NAME="prootmesa-${MESA_VERSION}-multiarch"
          ARCH_DESC="multiarch (aarch64 + armhf)"
        elif [ "$TARGET_ARCH" = "aarch64" ]; then
          PACKAGE_NAME="prootmesa-${MESA_VERSION}-aarch64"
          ARCH_DESC="aarch64"
        elif [ "$TARGET_ARCH" = "arm-linux-gnueabihf" ]; then
          PACKAGE_NAME="prootmesa-${MESA_VERSION}-armhf"
          ARCH_DESC="arm-linux-gnueabihf"
        fi
        
        # 打包完整安装目录
        cd /opt
        tar -czvf "${HOME}/${PACKAGE_NAME}-full.tar.gz" mesa/
        
        # 生成校验和
        cd $GITHUB_WORKSPACE
        mv "${HOME}/${PACKAGE_NAME}-full.tar.gz" .
        sha256sum ${PACKAGE_NAME}-*.tar.gz > checksums.txt
        
        # 设置输出变量
        echo "PACKAGE_NAME=${PACKAGE_NAME}" >> $GITHUB_ENV
        echo "FULL_PACKAGE=${PACKAGE_NAME}-full.tar.gz" >> $GITHUB_ENV
        echo "ARCH_DESC=${ARCH_DESC}" >> $GITHUB_ENV
        
        echo "📦 打包完成:"
        ls -lh *.tar.gz

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: prootmesa-${{ env.MESA_VERSION }}-${{ env.TARGET_ARCH }}
        path: |
          ${{ env.PACKAGE_NAME }}-*.tar.gz
          checksums.txt

    - name: 创建轻量级发布
      uses: softprops/action-gh-release@v1
      if: ${{ github.event_name == 'workflow_dispatch' }}
      with:
        tag_name: prootmesa-${{ env.MESA_VERSION }}-${{ env.TARGET_ARCH }}-${{ env.BUILD_DATE }}
        name: Mesa proot ${{ env.MESA_VERSION }} (${{ env.ARCH_DESC }})
        body: |
          Mesa 3D Graphics Library for Termux proot (proot)
          
          构建信息:
          - **版本**: ${{ env.MESA_VERSION }}
          - **架构**: ${{ env.ARCH_DESC }}
          - **Git 描述**: ${{ env.GIT_DESCRIBE }}
          - **提交哈希**: ${{ env.MESA_GIT_HASH }}
          - **构建日期**: ${{ env.BUILD_DATE }}
          - **分支**: ${{ env.MESA_BRANCH }}
          - **仓库**: $(if [ "${{ env.USE_UPSTREAM }}" = "true" ]; then echo "上游官方"; else echo "lfdevs/android-container"; fi)
          
          包含文件:
          - `${{ env.FULL_PACKAGE }}` - 完整 Mesa 安装包
          - `checksums.txt` - 校验和文件
          
          $(if [ "${{ env.TARGET_ARCH }}" = "both" ]; then
            echo "多架构包目录结构:"
            echo "```"
            echo "mesa/"
            echo "├── bin/                 # 可执行文件"
            echo "├── include/             # 头文件"
            echo "├── lib/                 # 库目录"
            echo "│   ├── aarch64-linux-gnu -> ../lib64/   # 64位库符号链接"
            echo "│   └── arm-linux-gnueabihf -> ../lib32/ # 32位库符号链接"
            echo "├── lib64/               # 64位 (aarch64) 库"
            echo "│   └── dri/             # 64位 DRI 驱动"
            echo "└── lib32/               # 32位 (armhf) 库"
            echo "    └── dri/             # 32位 DRI 驱动"
            echo "```"
          fi)
          
          安装说明:
          1. 下载完整的 tar.gz 包
          2. 在 proot 环境中解压到根目录: `tar -xzf package.tar.gz -C /`
          
          $(if [ "${{ env.TARGET_ARCH }}" = "both" ]; then
            echo "多架构使用说明:"
            echo "- 默认情况下，系统会自动选择合适的架构"
            echo "- 可以手动设置环境变量来选择特定架构:"
            echo "  ```bash"
            echo "  # 使用 64 位库"
            echo "  export LD_LIBRARY_PATH=/opt/mesa/lib64:/opt/mesa/lib64/dri:\$LD_LIBRARY_PATH"
            echo "  # 或者使用 32 位库"
            echo "  export LD_LIBRARY_PATH=/opt/mesa/lib32:/opt/mesa/lib32/dri:\$LD_LIBRARY_PATH"
            echo "  ```"
          fi)
          
          注意:
          - 需要 Termux 的 proot 环境 (如 prootdistro 的 Ubuntu/Debian)
          - 推荐在 proot 容器中使用
        files: |
          ${{ env.PACKAGE_NAME }}-*.tar.gz
          checksums.txt
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 输出总结
      run: |
        echo "✅ Mesa proot 构建完成!"
        echo "================================"
        echo "版本: $MESA_VERSION"
        echo "提交: $MESA_GIT_HASH"
        echo "构建日期: $BUILD_DATE"
        echo "分支: $MESA_BRANCH"
        echo "目标架构: $ARCH_DESC"
        echo "环境: Termux (proot)"
        echo ""
        echo "📦 生成的文件:"
        ls -1 *.tar.gz | while read file; do
          echo "  - $file"
        done
        echo ""
        echo "驱动支持:"
        echo "  ✓ Gallium: zink, freedreno, llvmpipe, softpipe"
        echo "  ✓ Vulkan: freedreno, swrast"
        echo "  ✓ OpenGL, OpenGL ES 1.x/2.x"
        echo "  ✓ EGL, GBM"
        echo ""
        if [ "$TARGET_ARCH" = "both" ]; then
          echo "多架构目录结构:"
          echo "  /opt/mesa/lib64/    - 64位 (aarch64) 库"
          echo "  /opt/mesa/lib32/    - 32位 (armhf) 库"
          echo "  /opt/mesa/lib/      - 符号链接到对应架构"
        fi